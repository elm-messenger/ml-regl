# API design

## OCaml -> JS (implemented in JS)

- `execCmd`: execute REGL commands

## JS -> OCaml (implemented in OCaml)

- `update_tick`: returns a view
- `receive_event`: no return
- `recevie_regl_msg`: no return
